# Projeto alvo: fcamarahorashub
# Após criar o projeto no dashboard (ou liberar cota free), execute:
#   supabase link --project-ref <ref>
#   supabase db push

# Substitua após: supabase link --project-ref <ref-do-fcamarahorashub>
# project_id = "<ref>"

[api]
enabled = true

[db]
major_version = 17
